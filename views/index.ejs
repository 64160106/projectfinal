<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lost and Found - Home</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css"> <!-- Custom styles -->
    <script src="https://cdn.jsdelivr.net/npm/moment@2.29.1/moment.min.js"></script> <!-- Moment.js -->
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">Lost and Found</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ml-auto">
                <% if (username) { %>
                    <li class="nav-item">
                        <span class="nav-link">Logged in as <%= username %></span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/create-post">Create Post</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">Register</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </nav>

    <div class="container mt-5">
        <h2>Lost and Found Posts</h2>
        <% if (posts.length > 0) { %>
            <div class="row">
                <% posts.forEach(post => { %>
                    <div class="col-md-4">
                        <div class="card mb-4">
                            <img class="card-img-top" src="/uploads/<%= post.image %>" alt="Image of <%= post.item_description %>">
                            <div class="card-body">
                                <h5 class="card-title"><%= post.item_description %></h5>
                                <p class="card-text">Location: <%= post.location %></p>
                                <p class="card-text">
                                    Found on: 
                                    <%= new Date(post.found_time).toLocaleString('en-GB', {
                                        day: 'numeric', month: 'long', year: 'numeric', hour: 'numeric', minute: 'numeric'
                                    }) %>
                                </p>
                                <p class="card-text">Posted by: <%= post.username %></p>
                                <p class="card-text">Status: <%= post.status %></p>

                                <!-- Check if the logged-in user is the post owner to show delete and update buttons -->
                                <% if (username === post.username) { %>
                                    <form action="/delete-post/<%= post.id %>" method="POST" class="mb-2">
                                        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                    </form>

                                    <!-- Show status change form for the post owner -->
                                    <form action="/update-status/<%= post.id %>" method="POST" class="mt-2">
                                        <select name="newStatus" class="form-control">
                                            <option value="searching" <%= post.status === 'searching' ? 'selected' : '' %>>กำลังตามหา</option>
                                            <option value="found" <%= post.status === 'found' ? 'selected' : '' %>>พบเจ้าของแล้ว</option>
                                            <option value="unclaimed" <%= post.status === 'unclaimed' ? 'selected' : '' %>>ยังไม่พบเจ้าของ</option>
                                        </select>
                                        <button type="submit" class="btn btn-success btn-sm mt-2">Update Status</button>
                                    </form>
                                <% } %>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p>No posts available. Be the first to create a post!</p>
        <% } %>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>