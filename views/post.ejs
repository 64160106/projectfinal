<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
</head>
<body>
    <h1>Posts</h1>
    <a href="/">หน้าแรก</a>
    <h2>โพสต์ทั้งหมด</h2>
    <ul>
        <% posts.forEach(post => { %>
            <li>
                <strong><%= post.description %></strong> - <%= post.location %> <br>
                <small>ผู้โพสต์: <%= post.user.username %></small> <br> <!-- แสดงชื่อผู้โพสต์ -->
                <small>เวลาที่พบ: <%= new Date(post.found_time).toLocaleString('th-TH', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit'
                }) %></small>
                <br>
                <% if (post.image) { %> <!-- ตรวจสอบว่ามีรูปภาพหรือไม่ -->
                    <img src="/uploads/<%= post.image %>" alt="โพสต์รูปภาพ" style="max-width: 300px; height: auto;">
                <% } %>
            </li>
        <% }) %>
    </ul>
</body>
</html>
