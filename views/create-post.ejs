<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>สร้างโพสต์</title>
    <link rel="stylesheet" href="/css/styles.css"> <!-- Link to your CSS file -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <header class="bg-primary text-white text-center py-4">
        <h1>สร้างโพสต์</h1>
        <nav>
            <ul class="nav justify-content-center">
                <li class="nav-item">
                    <a class="nav-link text-white" href="/">หน้าแรก</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="/logout">ออกจากระบบ</a>
                </li>
            </ul>
        </nav>
    </header>

    <main class="container mt-5">
        <div id="error-message" class="alert alert-danger d-none"></div> <!-- Error message section -->
        <form action="/create-post" method="POST" enctype="multipart/form-data" id="create-post-form">
            <div class="form-group">
                <label for="item_description">รายละเอียดของรายการ</label>
                <textarea id="item_description" name="item_description" class="form-control" rows="4" required></textarea>
            </div>
            <div class="form-group">
                <label for="location">สถานที่</label>
                <input type="text" id="location" name="location" class="form-control" required>
            </div>
            <div class="form-group">
                <label for="image">อัปโหลดรูปภาพ</label>
                <input type="file" id="image" name="image" class="form-control-file" accept="image/*" required>
                <small class="form-text text-muted">รองรับไฟล์ภาพประเภท JPEG, PNG, GIF ขนาดไม่เกิน 5MB</small>
            </div>
            <button type="submit" class="btn btn-primary">สร้างโพสต์</button>
        </form>
    </main>

    <footer class="text-center py-4 mt-5">
        <p>&copy; 2024 Lost & Found System</p>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    <script>
        // Display error messages if any
        const errorMessage = '<%= typeof errorMessage !== "undefined" ? errorMessage : "" %>';
        if (errorMessage) {
            $('#error-message').text(errorMessage).removeClass('d-none');
        }
    </script>
</body>
</html>
